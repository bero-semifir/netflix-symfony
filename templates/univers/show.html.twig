{% extends 'base.html.twig' %}

{% block title %}Univers{% endblock %}

{% block body %}
    <h1>Univers {{ univer.nom }}</h1>

    <table class="table">
        <tbody>
            {% if univer.films | length > 0  %}
            <tr>
                <th>Films de l'univers {{ univer.nom }}</th>
                {% for film in univer.films %}
                    <td><a href={{ url('app_film_show', { id: film.id }) }}>{{ film }}</a></td>                    
                {% endfor %}
            </tr>
            {% endif %}
            {% if univer.series | length > 0 %}
            <tr>
                <th>Séries de l'univers {{ univer.nom }}</th>
                {% for serie in univer.series %}
                    <td><a href={{ url('app_serie_show', { id: serie.id }) }}>{{ serie }}</a></td>                    
                {% endfor %}
            </tr>
            {% endif %}
        </tbody>
    </table>

    <a href="{{ path('app_univers_index') }}">back to list</a>

    <a href="{{ path('app_univers_edit', {'id': univer.id}) }}">edit</a>

    {{ include('univers/_delete_form.html.twig') }}
{% endblock %}
